# Database (SQLite local for MVP — switch to Postgres/Supabase in production)
DATABASE_URL="file:./dev.db"

# Auth session signing (change in production)
AUTH_SECRET="rfacto-dev-secret-change-me-in-production"

# Optional Supabase (Storage / Realtime / future Auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
